import React from 'react';
import { Section } from '../ui/Section';

export const PayrollPreview: React.FC = () => {
    const features = [
        "Full payroll processing",
        "RTI submissions to HMRC",
        "Auto-enrolment & pension management",
        "Digital payslips",
        "Year-end processing",
        "BACS bureau handling",
        "Dedicated account management"
    ];

    return (
        <Section background="green-light" id="pricing">
            <div className="grid grid-cols-1 lg:grid-cols-2 gap-10 lg:gap-20 items-center">

                {/* Content */}
                <div className="order-2 lg:order-1">

                    <h2 className="text-3xl md:text-4xl font-bold text-text-primary mb-6">
                        Payroll, Done Properly
                    </h2>
                    <p className="text-lg text-gray-600 mb-8">
                        Our payroll service is designed around three principles:<br />
                        <strong className="text-gray-900">Accuracy. Security. Efficiency.</strong>
                    </p>

                    <p className="text-md font-semibold text-gray-700 mb-6">For £3 per employee per month, we provide:</p>

                    <div className="grid grid-cols-1 sm:grid-cols-2 gap-y-3 gap-x-8">
                        {features.map((feature, index) => (
                            <div key={index} className="flex items-center gap-3">
                                <div className="w-5 h-5 rounded-full bg-green-primary flex items-center justify-center shrink-0">
                                    <svg width="12" height="12" viewBox="0 0 12 12" fill="none" xmlns="http://www.w3.org/2000/svg">
                                        <path d="M10 3L4.5 8.5L2 6" stroke="white" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" />
                                    </svg>
                                </div>
                                <span className="text-gray-600 font-medium text-sm md:text-base">{feature}</span>
                            </div>
                        ))}
                    </div>

                    <div className="mt-10 p-4 bg-white/50 rounded-lg border border-green-primary/10">
                        <p className="font-bold text-text-primary">Your employees are paid correctly and on time — every time.</p>
                    </div>
                </div>

                {/* Pricing Card */}
                <div className="order-1 lg:order-2 flex justify-center lg:justify-end">
                    <div className="bg-white rounded-2xl border-2 border-green-primary p-8 md:p-10 shadow-xl max-w-sm w-full text-center hover:scale-105 transition-transform duration-300">
                        <div className="text-sm font-bold text-gray-400 uppercase tracking-widest mb-2">Flat Rate</div>
                        <div className="flex items-baseline justify-center gap-1 mb-2">
                            <span className="text-6xl md:text-7xl font-bold text-green-primary">£3</span>
                        </div>
                        <div className="text-gray-500 font-medium border-t border-gray-100 pt-4 mt-4">
                            per employee / per month
                        </div>
                    </div>
                </div>
            </div>
        </Section>
    );
};