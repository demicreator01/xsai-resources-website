import React from 'react';
import { Section } from '../ui/Section';

export const WhyUs: React.FC = () => {
    const reasons = [
        "Transparent £5 per employee payroll",
        "No hidden payroll add-ons",
        "Segregated safeguarded client accounts",
        "Loyalty credits for early funding & volume",
        "Dedicated account manager",
        "Integrated payroll + accounting model",
        "Built for long-term partnerships"
    ];

    return (
        <Section background="dark" id="why-us">
            <div className="flex flex-col gap-12">
                <div className="max-w-3xl">

                    <h2 className="text-3xl md:text-4xl font-bold text-white mb-6">
                        Why Businesses Switch to Us
                    </h2>
                </div>

                <div className="grid grid-cols-1 md:grid-cols-2 gap-y-6 gap-x-12">
                    {reasons.map((reason, index) => (
                        <div key={index} className="flex items-center gap-4">
                            <div className="w-6 h-6 rounded-full bg-green-primary/20 flex items-center justify-center shrink-0">
                                <svg width="14" height="14" viewBox="0 0 12 12" fill="none" xmlns="http://www.w3.org/2000/svg">
                                    <path d="M10 3L4.5 8.5L2 6" stroke="currentColor" className="text-green-primary" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" />
                                </svg>
                            </div>
                            <span className="text-white text-lg font-medium">{reason}</span>
                        </div>
                    ))}
                </div>

                <div className="text-center pt-8 border-t border-gray-800 mt-8">
                    <p className="text-green-light font-semibold text-lg md:text-xl max-w-2xl mx-auto leading-relaxed mb-4">
                        We are not a volume call centre.
                    </p>
                    <p className="text-gray-400 italic text-lg max-w-2xl mx-auto leading-relaxed">
                        "We build stable relationships with businesses that value structure and fairness."
                    </p>
                </div>
            </div>
        </Section>
    );
};
